doctype html
html
	head
		script(src='/js/jquery-1.10.1.js')
		
	body
		h1 #{user}'s page

		if chars.length > 0

			p Characters:
			ul#charList
				each char in chars
					li
						a(href='/#{user}/#{char.canon_name}') #{char.name}
		else
			p This user lacks character (badum-ching!)
